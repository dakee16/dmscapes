# Step 1 "Draw your own room" screenshot

The Design-your-room section on /plan (Step 1) shows a screenshot of the drawing
tool. Drop your image here and it replaces the placeholder automatically:

- draw-your-room.png   (a screenshot of the /plan/draw drawing tool in use)

4:3 fills the slot best (it is object-cover, so other ratios crop). Until the
file exists, the slot shows a "Drawing tool preview" placeholder.
